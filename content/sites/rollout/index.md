# Rollout

Rollout is a CLI-first [static site hosting](https://rollout.run) platform. Deploy your static sites in seconds on cloudflare's global CDN via `rollout deploy`. Custom domains, instant SSL, global edge.

**Pricing:** Free
**Website:** [rollout.run](https://rollout.run)
